<h1>Users</h1>
<table class="table table-hover table-inverse">
  <thead>
    <th>image
    </th>
    <th>name
    </th>
    <th>
    </th>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <th>
          <%= attachment_image_tag user, :profile_image, :fill, 20, 20, fallback: "no_image.jpg", size:'30x30' %>
        </th>
        <th><%= user.name %></th>
        <th><%= link_to 'Show', user_path(user) %></th>
      </tr>
    <% end %>
  </tbody>
</table>
<div>